func add(x, y) local (a, b)
  (
{ T }
    a = x;
{ T }
    b = y;
{ T }
    if a > 0
      (
{ -a + 1 ≤ 0 }
        a = a - 1;
{ -a ≤ 0 }
        b = b + 1;
        b = add(a, b)
{ T }
      );
    return b
{ 1 ≤ 0 }
  )

{ T }
x = add(y, z);
{ T }
x = add(x, 100);
{ T }
x = add(x, x);
while x > 0
{ -x + 1 ≤ 0 }
  x = x - 1
{ x ≤ 0 }
