func iter(x) local (t)
  (
{ T }
    t = x;
{ -t + x ≤ 0 /\ t - x ≤ 0 }
    while x > 0
{ -x + 1 ≤ 0 /\ -t + x ≤ 0 }
      x = x - 1;
    return t
{ 1 ≤ 0 }
  )

{ T }
x = iter(x);
{ T }
x = iter(x);
while x < 1
{ x ≤ 0 }
  x = x + 2
{ -x + 1 ≤ 0 }
