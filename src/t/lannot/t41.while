func f(x, y) local (a)
  (
{ T }
    assert z >= 0;
    if x + z < y
      (
{ x - y + z + 1 ≤ 0 /\ -z ≤ 0 }
        x = x + 1;
{ x - y + z ≤ 0 /\ -z ≤ 0 }
        a = x;
{ a - y + z ≤ 0 /\ x - y + z ≤ 0 /\ -z ≤ 0 }
        a = a + z;
{ a - y ≤ 0 /\ x - y + z ≤ 0 /\ -z ≤ 0 }
        f(x, a);
{ T }
        f(a, y);
        ()
{ T }
      )
{ T }
  )

f(x, y)
{ T }
