func swap() local (t)
  (
{ T }
    t = x;
{ -t + x ≤ 0 /\ t - x ≤ 0 }
    x = y;
    y = t
{ t - y ≤ 0 /\ -t + y ≤ 0 }
  )

{ T }
while x < 0
{ x + 1 ≤ 0 }
  x = x + 1;
{ -x ≤ 0 }
swap();
while x > 0
{ -x + 1 ≤ 0 }
  x = x - 1
{ x ≤ 0 }
