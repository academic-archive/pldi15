
func count_down (x) local (a)
( a = x;
  if a > 0 (
    a = a-1;
    count_down(a) )
)

func copy (x,y) local (a,b)
( a = x;
  b = y;
  if a > 0 (
    b = b+1;
    a = a-1;
    copy (a,b)
  ) else (
    return b )
)


y = copy (x,y);

count_down(y)
